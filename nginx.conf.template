events {
    worker_connections 1024;
}

http {
    upstream backend {
        server $CONTAINER weight=1;
    }

    server {
        listen 80;
        server_name $DOMAIN;

        location ~ ^/api/(.*) {
            proxy_pass http://backend/$1$is_args$args;
        }
    }
}